import { NavigationContainer } from '@react-navigation/native';

import { AppRoute } from './app.routes';

export const Routes = () => {
  return (
    <NavigationContainer>
      <AppRoute />
    </NavigationContainer>
  )
}
